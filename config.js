/**
 * Platinum Pine - Configuration
 * Replace these values before going live
 */
const CONFIG = {
  // Formspree: Only the form ID (e.g. mreawvgq), not the full URL
  formspreeId: 'mreawvgq',

  // Razorpay: Key ID from dashboard (rzp_test_xxx or rzp_live_xxx)
  razorpayKeyId: 'rzp_live_rFsMdUTSqaS9uW',
  // Razorpay Secret: Set in Netlify env vars as RAZORPAY_KEY_SECRET (never put in code!)
  // Create Order API: /.netlify/functions/create-order (when deployed on Netlify)

  // Meta Pixel: Your Facebook Pixel ID
  metaPixelId: '758574410251016'
};
